FROM python:3.12-slim

WORKDIR /app

RUN pip install uv --quiet

COPY pyproject.toml .
COPY src/ src/

RUN uv pip install --system -e .

EXPOSE 8000

CMD ["uvicorn", "listings.main:app", "--host", "0.0.0.0", "--port", "8000"]
